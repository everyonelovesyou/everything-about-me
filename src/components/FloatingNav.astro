---
const base = import.meta.env.BASE_URL;
const navItems = [
  { label: 'Top', href: `${base}/` },
  { label: 'About', href: `${base}/about/` },
  { label: 'Works', href: `${base}/works/` },
  { label: 'Blog', href: `${base}/blog/` },
  { label: 'Contact', href: `${base}/contact/` },
];
---

<nav class="floating-nav">
  <button id="nav-btn" class="nav-btn" popovertarget="nav-menu">✦</button>
  <div id="nav-menu" class="nav-menu" popover>
    <ul>
      {
        navItems.map((item) => (
          <li>
            <a href={item.href}>{item.label}</a>
          </li>
        ))
      }
    </ul>
  </div>
</nav>

<style is:global lang="scss">
  .floating-nav {
    --_offset: 2rem;
    --_btn-size: 3.2rem;
  }
</style>

<style lang="scss">
  @use '/src/styles/mixin/_hover' as *;

  .nav-btn {
    font-size: 1.3rem;
    width: var(--_btn-size);
    height: var(--_btn-size);
    border-radius: 50%;
    display: grid;
    place-items: center;

    &:not(:disabled) {
      border: 2px solid #f4c2d7;
      background-color: rgb(from #fff r g b / 0.9);
      box-shadow: 0 4px 16px rgba(244, 194, 215, 0.35);
      cursor: pointer;

      transition-duration: 0.2s;
      transition-timing-function: ease;
      transition-property: translate, box-shadow;

      @include hover('&:has(+ :popover-open)') {
        translate: 0 -3px;
        box-shadow: 0 6px 20px rgba(244, 194, 215, 0.5);
      }
    }
  }

  .nav-menu {
    background-color: rgb(from #fff r g b / 0.9);
    backdrop-filter: blur(12px);
    border-radius: 1rem;
    border: 1px solid #f4c2d7;
    box-shadow: 0 8px 32px rgba(244, 194, 215, 0.25);
    padding: 0.5rem;
    width: auto;
    height: auto;
    min-width: 9rem;
    inset: unset;
    bottom: calc(anchor(top) + 1rem);
    right: anchor(right);

    @supports not (right: anchor(right)) {
      bottom: calc(var(--_offset) + var(--_btn-size) + 1rem);
      right: calc(var(--_offset) + var(--_btn-size));
    }
  }

  .nav-menu a {
    display: block;
    padding: 0.5rem 1rem;
    border-radius: 0.6rem;
    font-size: 0.88rem;
    font-weight: 500;
    color: #6b5b6e;
    letter-spacing: 0.02em;

    &:any-link {
      transition: background-color 0.15s ease;

      @include hover() {
        background-color: #fef0f5;
        color: #d4789c;
      }
    }
  }
</style>
